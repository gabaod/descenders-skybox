Welcome to a skybox for unity 2017 using custom shaders and scripts

V1 directory is version 1 which allows you to define your own setup
V2 directory is version 2 which allows a time of day setting using pre defined colors 
